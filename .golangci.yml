version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosec
    - gocritic
  settings:
    gosec:
      excludes:
        - G115

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
